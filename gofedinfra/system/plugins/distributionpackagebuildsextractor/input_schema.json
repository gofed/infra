{
	"$schema": "//json-schema.org/draft-04/schema#",
	"description": "Distribution package builds extractor plugi",
	"title": "Distribution package builds extractor plugin",
	"type": "object",
	"properties": {
		"package": {
			"description": "Distribution package",
			"type": "string"
		},
		"product": {
			"description": "Product name",
			"type": "string"
		},
		"distribution": {
			"description": "Distribution name",
			"type": "string"
		},
		"start_date": {
			"description": "Search for builds not older than",
			"type": "string"
		},
		"end_date": {
			"description": "Search for builds not younger than",
			"type": "string"
		},
		"start_timestamp": {
			"description": "Search for builds not older than",
			"type": "integer"
		},
		"end_timestamp": {
			"description": "Search for builds not younger than",
			"type": "integer"
		}
	},
	"required": ["package", "product", "distribution"]
}
